# Local credentials — NEVER commit. Real values stay on your machine only.
.env
.env.local
.env.enterprise
.env.*.local

# Local credential fallback files (if the skill ever grows one)
data/credentials.json
config.local.json

# Python build artefacts
__pycache__/
*.py[cod]
*$py.class
*.pyo

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/

# OS noise
.DS_Store
Thumbs.db

# Pandoc / DOCX byproducts (per-doc, do not commit)
*.for_word.md
