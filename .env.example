# Credential template for skill-markdown-to-word.
#
# This skill does NOT call any external API and does NOT require any secret.
# The file is kept only to follow the shared repository template convention
# of the surrounding skill collection.
#
# If you extend this skill with a step that needs a credential, declare the
# variable name here with an empty value and load it at runtime using:
#
#   import os
#   from dotenv import load_dotenv, find_dotenv
#   try:
#       load_dotenv(find_dotenv(usecwd=True))
#   except ImportError:
#       pass
#   api_token = os.environ.get("EXAMPLE_API_TOKEN", "")
#
# Then commit this file (with empty values) and keep your real `.env`
# locally — `.env` is already ignored by `.gitignore`.
